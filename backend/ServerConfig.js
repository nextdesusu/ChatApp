const siteName = "localhost";
const serverPort = 3001;
const clientPort = 3000;
const dbRoute = 'mongodb://localhost/chatAppDB';

module.exports = { 
	SITE_NAME: siteName,
	SERVER_PORT: serverPort, 
	CLIENT_PORT: clientPort,
	DB_ROUTE: dbRoute,
}